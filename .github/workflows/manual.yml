# This is a basic workflow that is manually triggered

name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch


jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - uses: actions/checkout@v1

      - name: Cache node modules
        uses: actions/cache@v1
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - name: Node ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: npm ci and npm run build
        run: |
          npm ci
          npm run build --prod

      - name: debug
        run: |
          echo ${{ secrets.DEPLOY_SERVERNAME }}
          echo ${{ secrets.DEPLOY_USERNAME }}
          echo "${{ secrets.DEPLOY_PASSWORD }}"

      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: home155661940.1and1-data.host
          username: u39901709-deploy-wky
          password: "PpfYMpXI5Z4m!"
          local-dir: ./dist/WhiskeyTrader/
          log-level: verbose
